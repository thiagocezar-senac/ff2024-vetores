// vetor[0] = "Nome 1"; - Adiciona valor ao array
// vetor.push("Nome 2"); - Adiciona valor na última posição
// vetor.pop(); - Trás o último valor do array e exclui o mesmo

// Declaro vetor
let vetor = [];

// Adiciono por posição
vetor[0] = "Nome 1";

// Adiciono na última posição
vetor.push("Nome 2");


// vetor.push(prompt("Insira um valor"));

console.log(vetor);
